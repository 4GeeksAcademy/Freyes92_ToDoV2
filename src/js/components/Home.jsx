import React from "react";
import TodoList from "./TodoList";

const Home = () => {
    return (
        <div className="home-container">
            <TodoList />
        </div>
    );
};

export default Home;